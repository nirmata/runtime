# Invalid: a behavior item that declares no behavior kind at all.
apiVersion: runtime.nirmata.io/v1alpha1
kind: RuntimePolicy
metadata:
  name: chainsaw-invalid-zero-kinds
spec:
  mode: enforce
  podSelector: {}
  behaviors:
  - {}
