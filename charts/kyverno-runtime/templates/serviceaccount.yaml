{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kyverno-runtime.serviceAccountName" . }}
  labels:
    {{- include "kyverno-runtime.labels" . | nindent 4 }}
{{- end }}
