---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: runtimepolicies.runtime.kyverno.io
spec:
  group: runtime.kyverno.io
  names:
    kind: RuntimePolicy
    listKind: RuntimePolicyList
    plural: runtimepolicies
    shortNames:
    - rpol
    singular: runtimepolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .status.conditions[?(@.type=="Applied")].status
      name: Applied
      type: string
    - jsonPath: .status.conditions[?(@.type=="Applied")].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RuntimePolicySpec defines a runtime policy for enforcing
              or monitoring behaviors.
            properties:
              behaviors:
                description: Behaviors defines the allowed and denied runtime behaviors.
                items:
                  description: PolicyBehavior defines allow/deny rules for a specific
                    behavior type.
                  properties:
                    exec:
                      description: Exec defines command execution behavior rules.
                      properties:
                        allow:
                          description: Allow specifies allowed network access.
                          properties:
                            expression:
                              description: Expression is a CEL expression that evaluates
                                to a list of items.
                              type: string
                            values:
                              description: Values is a list of allowed or denied items
                                (IPs, commands, files, etc.).
                              items:
                                type: string
                              type: array
                          type: object
                        deny:
                          description: Deny specifies denied network access.
                          properties:
                            expression:
                              description: Expression is a CEL expression that evaluates
                                to a list of items.
                              type: string
                            values:
                              description: Values is a list of allowed or denied items
                                (IPs, commands, files, etc.).
                              items:
                                type: string
                              type: array
                          type: object
                      type: object
                    network:
                      description: Network defines network behavior rules.
                      properties:
                        allow:
                          description: Allow specifies allowed network access.
                          properties:
                            expression:
                              description: Expression is a CEL expression that evaluates
                                to a list of items.
                              type: string
                            values:
                              description: Values is a list of allowed or denied items
                                (IPs, commands, files, etc.).
                              items:
                                type: string
                              type: array
                          type: object
                        deny:
                          description: Deny specifies denied network access.
                          properties:
                            expression:
                              description: Expression is a CEL expression that evaluates
                                to a list of items.
                              type: string
                            values:
                              description: Values is a list of allowed or denied items
                                (IPs, commands, files, etc.).
                              items:
                                type: string
                              type: array
                          type: object
                      type: object
                    open:
                      description: Open defines file open behavior rules.
                      properties:
                        allow:
                          description: Allow specifies allowed network access.
                          properties:
                            expression:
                              description: Expression is a CEL expression that evaluates
                                to a list of items.
                              type: string
                            values:
                              description: Values is a list of allowed or denied items
                                (IPs, commands, files, etc.).
                              items:
                                type: string
                              type: array
                          type: object
                        deny:
                          description: Deny specifies denied network access.
                          properties:
                            expression:
                              description: Expression is a CEL expression that evaluates
                                to a list of items.
                              type: string
                            values:
                              description: Values is a list of allowed or denied items
                                (IPs, commands, files, etc.).
                              items:
                                type: string
                              type: array
                          type: object
                      type: object
                  type: object
                  x-kubernetes-validations:
                  - message: exactly one of network, exec, or open must be specified
                    rule: '(has(self.network) ? 1 : 0) + (has(self.exec) ? 1 : 0)
                      + (has(self.open) ? 1 : 0) == 1'
                type: array
              evaluationInterval:
                description: EvaluationInterval specifies how frequently the policy
                  is re-evaluated.
                type: string
              mode:
                description: Mode defines the operational mode of the policy.
                enum:
                - monitor
                - enforce
                type: string
              podSelector:
                description: PodSelector identifies the pods this policy applies to.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              variables:
                description: Variables defines custom variables that can be used in
                  behavior expressions.
                items:
                  description: Variable is the definition of a variable that is used
                    for composition. A variable is defined as a named expression.
                  properties:
                    expression:
                      description: |-
                        expression is the expression that will be evaluated as the value of the variable.
                        The CEL expression has access to the same identifiers as the CEL expressions in Validation.
                      type: string
                    name:
                      description: |-
                        name is the name of the variable. The name must be a valid CEL identifier and unique among all variables.
                        The variable can be accessed in other expressions through `variables`
                        For example, if name is "foo", the variable will be available as `variables.foo`
                      type: string
                  required:
                  - expression
                  - name
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
            type: object
          status:
            description: RuntimePolicyStatus reflects the observed state of the policy.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastEvaluatedTime:
                description: LastEvaluatedTime is the last time the policy was evaluated.
                format: date-time
                type: string
              nodes:
                description: Nodes holds one per-node shard, each written only by
                  that node's daemon.
                items:
                  description: |-
                    NodePolicyStatus is one node's shard of a RuntimePolicy's status, written
                    only by that node's daemon.
                  properties:
                    lastEvaluatedTime:
                      format: date-time
                      type: string
                    nodeName:
                      type: string
                  required:
                  - nodeName
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - nodeName
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
